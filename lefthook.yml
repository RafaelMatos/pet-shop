pre-commit:
  commands:
    check:
      run: pnpm format {staged_files}
      glob: '*.{ts,tsx,js,jsx,json,css,.mjs,md}'

pre-push:
  commands:
    validate:
      glob: '*.{ts,tsx}'
      run: pnpm validate:typecheck
